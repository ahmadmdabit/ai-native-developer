# 🧠 مكتبة الأوامر (Prompt Library)

مجموعة منسقة من الأوامر (Prompts) القابلة لإعادة الاستخدام وعالية الجودة لوكلاء الذكاء الاصطناعي عبر دورة حياة تطوير البرمجيات. استخدمها كما هي أو قم بتخصيصها لتناسب مكدسك التقني (Stack).

---

## 📦 توليد الكود (Code Generation)

### توليد مكون React

```text
اكتب مكون React وظيفي (Functional Component) باسم `{{ComponentName}}` يقبل الخصائص (Props) التالية: {{PropsList}}. يجب أن يقوم بتصيير (Render) {{ComponentBehavior}}. استخدم واجهات TypeScript (Interfaces) للخصائص. تأكد من أن المكون متوافق مع إمكانية الوصول (A11y compliant) ويعالج حالات التحميل/الخطأ (Loading/Error states).
```

### إنشاء نقطة نهاية لـ REST API

```text
ولد معالج مسار (Route Handler) لـ Express.js لـ `POST /{{endpoint}}` يقبل حمولة JSON (JSON Payload) تحتوي على {{fields}} ويخزنها في مجموعة MongoDB باسم `{{collectionName}}`. قم بتضمين التحقق من صحة المدخلات باستخدام Zod ومعالجة الأخطاء بشكل صحيح.
```

---

## 🧪 توليد الاختبارات (Test Generation)

### اختبارات الوحدة (Unit Tests) لدالة

```text
اكتب اختبارات وحدة باستخدام Jest لدالة JavaScript التالية. قم بتغطية الحالات الحدية (Edge cases)، والمدخلات غير الصالحة، والمسارات السعيدة (Happy paths). تأكد من تغطية الفروع (Branch coverage) بنسبة 100%. قم بمحاكاة (Mock) أي اعتماديات خارجية.
```

### اختبار التكامل (Integration Test) لـ API

```text
ولد اختبار تكامل باستخدام Supertest لنقطة النهاية `GET /api/{{resource}}`. تحقق من حالة الاستجابة (Status)، ومخطط JSON (JSON Schema)، وسيناريوهات الخطأ (401, 404, 500).
```

---

## 🧹 إعادة الهيكلة والتحسين (Refactoring & Optimization)

### إعادة الهيكلة من أجل المقروئية

```text
أعد هيكلة (Refactor) الكود التالي لتحسين المقروئية وقابلية الصيانة. استخدم أسماء متغيرات واضحة، واستخرج دوال مساعدة (Helper functions)، وأزل التكرار (مبدأ DRY). لا تغير منطق العمل (Business logic).
```

### التحسين من أجل الأداء

```text
حسن الدالة التالية من أجل الأداء. حدد نقاط الاختناق (Bottlenecks) (O(n^2) أو أسوأ)، وتجنب الحسابات غير الضرورية، وقلل التعقيد الزمني (Time complexity) حيثما أمكن. اشرح تغييراتك.
```

---

## 📚 التوثيق (Documentation)

### سلسلة توثيق الدالة (Function Docstring)

```text
اكتب سلسلة توثيق (Docstring) لـ Python للدالة التالية باستخدام تنسيق Google-style. قم بتضمين وصف، والوسائط (Arguments)، والقيمة المرجعة (Return value)، والاستثناءات المثارة (Raises)، وكتلة مثال للاستخدام.
```

### توثيق المكون (Component Documentation)

```text
ولد توثيق Markdown لمكون React المسمى `{{ComponentName}}`. قم بتضمين:
1. الوصف
2. جدول الخصائص (Props table) (الاسم، النوع، الوصف)
3. مثال للاستخدام
4. الحالات الحدية (Edge cases)
```

---

## ⚙️ ديف أوبس و CI/CD

### تدفق عمل GitHub Actions

```text
أنشئ تدفق عمل GitHub Actions يقوم بتشغيل الاختبارات عند الدفع (Push) إلى `main`، ويبني التطبيق، وينشره على GitHub Pages. قم بتضمين التخزين المؤقت (Caching) لـ `node_modules` لتسريع عمليات البناء.
```

### مولد Dockerfile

```text
ولد ملف Docker (Dockerfile) متعدد المراحل لتطبيق Node.js. المرحلة 1: البناء. المرحلة 2: تشغيل الإنتاج (باستخدام `npm start`). قم بتضمين أفضل الممارسات للتخزين المؤقت للطبقات (Layer caching) والتشغيل كمستخدم غير جذري (Non-root user) للأمان.
```

---

## 🛡️ الأمان والتحقق (Security & Validation)

### تعقيم المدخلات (Input Sanitization)

```text
أضف التحقق من صحة المدخلات وتعقيمها إلى مسار Express.js التالي لمنع هجمات XSS وهجمات حقن SQL/NoSQL. استخدم مكتبة تحقق مثل `express-validator` أو `Joi`.
```

### تدفق مصادقة آمن

```text
ولد تدفق تسجيل دخول آمن باستخدام JWTs في واجهة خلفية Node.js + Express. قم بتضمين تجزئة كلمة المرور (Hashing) باستخدام (bcrypt)، وانتهاء صلاحية الرمز المميز (Token expiration)، ومنطق رمز التحديث (Refresh token).
```

---

## 🧠 وصف هندسة الأوامر (Prompt Engineering Meta)

### تصحيح الأوامر (Prompt Debugging)

```text
حلل الأمر التالي واقترح تحسينات لجعله أكثر تحديداً، وحتمية (Deterministic)، وأماناً. حدد أي غموض قد يؤدي إلى هلوسات (Hallucinations).
```

---

## 📌 نصائح

- استخدم الأقواس المتعرجة المزدوجة `{{ }}` للعناصر النائبة (Placeholders).
- اجعل الأوامر قصيرة، ومحددة، وموجهة نحو الهدف.
- قم بتضمين القيود (مثل اللغة، إطار العمل، التنسيق) عند الحاجة.
