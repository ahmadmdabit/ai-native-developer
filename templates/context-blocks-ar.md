# 🧱 كتل السياق (Context Blocks)

مقتطفات سياق قابلة لإعادة الاستخدام لتغذية وكلاء الذكاء الاصطناعي (AI Agents) من أجل تأريض (Grounding)، ودقة، واتساق أفضل.

---

## 🧩 البيانات الوصفية للمشروع (Project Metadata)

```json
{
  "project": "MyApp",
  "framework": "React 18",
  "language": "TypeScript",
  "stateManagement": "Redux Toolkit",
  "testing": ["Jest", "React Testing Library"],
  "deployment": "GitHub Pages"
}
```

---

## 🎨 دليل النمط (Style Guide)

```markdown
- استخدم camelCase للمتغيرات والدوال (Functions)
- استخدم PascalCase للمكونات (Components) والفئات (Classes)
- فضل الدوال السهمية (Arrow functions) على تصريحات الدوال التقليدية
- استخدم `const` و `let` (لا تستخدم `var`)
- قم دائماً بتضمين واجهات TypeScript (Interfaces) للـ Props والـ State
- تجنب أنواع `any`؛ استخدم أنواعاً محددة أو Generics
```

---

## 🔌 مرجع واجهة برمجة التطبيقات (API Reference)

```yaml
GET /api/users
- يرجع قائمة بالمستخدمين
- يتطلب ترويسة Authorization
- الاستجابة: 200 OK
  [
    {
      "id": "string",
      "name": "string",
      "email": "string"
    }
  ]
```

---

## 🧪 استراتيجية الاختبار (Testing Strategy)

```markdown
- اختبارات الوحدة (Unit tests) لجميع دوال الخدمات (Utility functions) والمكونات
- اختبارات التكامل (Integration tests) لنقاط نهاية الـ API
- اختبارات من النهاية إلى النهاية (E2E) لتدفقات المستخدم الحرجة
- مطلوب تغطية اختبار (Test coverage) بنسبة 90%+
- محاكاة (Mock) الخدمات الخارجية باستخدام MSW أو Jest mocks
```

---

## 🔐 سياسة الأمان (Security Policy)

```markdown
- تعقيم (Sanitize) جميع مدخلات المستخدم
- استخدام HTTPS لجميع نقاط النهاية
- تخزين الأسرار (Secrets) في متغيرات البيئة (لا تضعها أبداً في الكود)
- استخدام JWTs مع انتهاء الصلاحية ورموز التحديث (Refresh tokens)
- التحقق من صحة جميع حمولات الـ API (Payloads) مقابل مخطط (Schema) (مثل Zod)
```

---

## 🧠 مثال على سياق الأمر (Prompt Context Example)

```markdown
أنت وكيل تكويد (CoderAgent) تعمل على مشروع React + TypeScript. مهمتك هي توليد مكون جديد بناءً على المواصفات التالية. اتبع دليل النمط وتأكد من أن الكود قابل للاختبار ومقروء.
```

---

## 📌 نصائح

- حافظ على كتل السياق أقل من 1,000 رمز (Token) عندما يكون ذلك ممكناً.
- استخدم تنسيقات مهيكلة (JSON, YAML, Markdown) للوضوح.
- قم بتحديث كتل السياق مع تطور المكدس التقني (Stack) الخاص بك.
